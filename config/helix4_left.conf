# Copyright (c) 2020 The ZMK Contributors
# SPDX-License-Identifier: MIT

# Enable OLED display on central (left) side
CONFIG_ZMK_DISPLAY=y

# Reduce split link slave latency (default 30 → 0)
# Prevents the peripheral from skipping split BLE connection events
CONFIG_ZMK_SPLIT_BLE_PREF_LATENCY=0
